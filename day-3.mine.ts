// Given an array of numbers, find the maximum sum of any contiguous subarray of the array.

function maxSubarraySum(givenArray: number[]) {
	for (const fact of givenArray) {
		// I couldn't find fix.
